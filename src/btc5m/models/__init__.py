"""Settlement-probability models.

Models estimate calibrated P(YES=1). Calibration is mandatory; accuracy alone is
not a sufficient evaluation. Until a trained, calibrated model exists, only the
neutral baseline is available and outputs stay in WATCH / MANUAL_REVIEW.
"""
