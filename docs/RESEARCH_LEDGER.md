# Research ledger — every hypothesis, its result, and its status

> One line of record per research leg: what was tested, where it reproduces, and
> the verdict. Every entry links to the module or command that reproduces it.
> Dates below are the research timeline; the most recent legs are from 2026-06-15.

## How to read this
- **CONCLUDED-NEGATIVE** = tested honestly, no edge after costs at the data
  resolution we have. Don't re-run it on more of the same data expecting a
  different answer; re-open only if the *inputs* change (better data, fees, venue).
- **OPEN** = promising or unresolved; the blocking input is named.
- **INFRA** = measurement/safety capability, not an edge claim.

## The one-paragraph summary
KXBTC15M is a tight, well-calibrated market: median spread ~1c, taker fee ~1.5c,
market-implied probability is the best-calibrated forecaster we have measured
(distinct-window ECE ≈ 0.02, vs 0.03+ for every model we trained). Every taker
strategy tested — direct model, recalibrated model, residual-over-market models,
stale-quote sniping — fails to clear the ~2.5c round cost out-of-sample. The
conservative lower bound on maker (passive) entries is also negative, but that
bound is provably pessimistic and can only be tightened with trade prints and
sub-second WebSocket book data — both of which are RECORDING as of 2026-06-10
(API credentials were already configured in `.env`; the WS delivers ~410 book
updates/s at 14ms median receive age). Those studies, or a structural change
(cross-venue, different series), are where remaining edge could live. The
system itself is healthy: it measures honestly and fails fast.

## Ledger

| # | Hypothesis / leg | Where | Result | Status |
|---|---|---|---|---|
| 1 | Polymarket BTC 5m Up/Down pipeline (original project) | git history (removed 2026-06-10); venue semantics in `docs/archive/` | Full record/label/feature pipeline worked; discovery bug found+fixed; venue parked for account/funding reasons before model evidence | PARKED (code removed; restorable from git) |
| 2 | Kalshi KXBTC15M collection + OFFICIAL labeling | `kalshi-collect-continuous`, `kalshi-data-readiness` | 770 usable gate windows, ~240k book snapshots, ~700k underlying rows; both train (150) and backtest (60) gates passed | INFRA — keep running |
| 3 | A model on book+microstructure features beats the Kalshi price (taker) | `kalshi-train-baselines`, `kalshi-backtest-baselines` | AUC 0.90 but market-implied is better calibrated (window-ECE 0.020 vs 0.031+); fitted baselines LOSE after fees at the ask; market-implied trades 0 by construction | CONCLUDED-NEGATIVE |
| 4 | Isotonic calibration makes the model tradable | `kalshi-calibration-report`, memory: calibration-buffer audit | June-3 isotonic OVERFIT (test ECE 0.040→0.076), created a real bias-dominated 5–16c edge buffer; fixed 2026-06-10 by retrain+repromote on 770 windows (ECE 0.058→0.026, overfit_risk low) | RESOLVED (hygiene, not edge) |
| 5 | Confidence-aware edge policy (Wilson buffers, reservation prices) | `kalshi-edge-policy-report`, `kalshi-uncertainty-audit` | Buffers are mathematically correct and bias-dominated; policy correctly blocks ~all candidates (1/137 positive final edge in shadow) | INFRA — working as designed |
| 6 | Shadow "passes" indicate edge | `shadow_compare` ledgers 06-06→06-08 | Pass rows = regime luck: wins track realized window direction; fades 0/5; "identity knife-catches down windows" (14% win) | CONCLUDED-NEGATIVE |
| 7 | Trade more / trade less (frequency frontier) | `kalshi-frequency-report` + sweeps | Marginal trades stop adding value almost immediately; distinct windows matter more than trade count; overtrading destroys | INFRA — informs caps |
| 8 | Kalshi quotes lag BTC moves (stale-quote, v1 at ~4s data) | `kalshi-reprice-lag-study` | NULL: detected "opportunities" lose; median lag 0c; 4s cadence cannot even see the hypothesis | CONCLUDED-NEGATIVE at this resolution |
| 9 | Sub-second measurement layer | `kalshi-hires-record`, `hires/` | Works: Binance ~27ms, Coinbase ~120ms ticks; Kalshi REST book ~1.1s (RTT-bound) | INFRA |
| 10 | Stale-quote v2 on hi-res joins | `kalshi-reprice-lag-study --hires` | NEGATIVE: win 0%, no fee-surviving edge; +250/500ms response horizons ~0% covered because the Kalshi book is REST-polled at ~1.1s | UNBLOCKED 2026-06-10: WS book source live (~410 upd/s); re-run after hi-res WS data accumulates |
| 11 | Residual-over-market models (predict y − p_market) | `kalshi-train-residual-models` | No model stably beats market OOS (walk-forward deltas flip sign; ICs flip sign); lightgbm's +2.14 single-split backtest has the WORST calibration (ECE 0.070) = luck | CONCLUDED-NEGATIVE |
| 12 | Replace the overfit calibrator with a safer one | `kalshi-calibrator-replacement-review`, `kalshi-paper-calibrator-swap-review` | Fresh isotonic ≈ break-even (+0.73 over 179 test windows); market-implied best calibrated (0.0199) but is the price (no edge); swap gate declined all candidates; superseded by full re-promotion 2026-06-10 | RESOLVED |
| 13 | Maker (passive) entries recover the spread+fee | `kalshi-maker-entry-study --fill-model prints-through\|prints-front` | RESOLVED WITH REAL FILLS (3.2M backfilled prints, 552-581 windows): maker saves ~1.8c vs taker per fill (real), but STATIC side-picking is pure drift exposure — per-side EV flips sign daily with BTC direction (YES −3.7/−2.0c, NO +0.0/+1.4c through/front brackets in a down week). Favorite-band cells positive on both bounds (YES 80c+: +1.6/+2.5c; NO 40-90c band) but drift-confounded | PARTIAL — execution-cost question answered; side-selection needs a signal or more regimes |
| 14 | Both-sides quoting (one-shot, join-bid both legs) | same study, real fills | DECISIVELY NEGATIVE: full P&L −3.15c per quote point, negative 10/10 days. 74.9% double fills lock +0.97c, but naked single-fill legs average ≈ −21c (the market ran through one side) — static quoting cannot survive; viable MM needs fast cancel/requote (WS) + inventory skew | CONCLUDED-NEGATIVE for static quoting |
| 15 | Deep-favorite YES bias (market underprices YES overall by ~1.2c; YES at 90c+ shows maker +2.05c, taker +0.96c over 262 windows) | maker study by-price table; calibrator review; **ADE maker mine + autopsy 2026-06-15** | Only consistently positive cell across studies. **ADE maker mine rediscovered it independently and it PERSISTS ON THE SEALED HOLDOUT** (rest YES bid on 96c+ favorites: +2.65c/fill prints-through, +1.77c front-queue, win 100%, t>6 OOS) — passed DSR 0.998 + PBO 0.00. BUT it is **short-vol favorite-capture**: ~2-4c collected vs a −96c single-loss tail (100% win = no tail in 67/24-fill sample, not a missing tail); DSR is blind to the unrealized left tail; fills optimistic (queue); plateau-fragile. NOT bankable | RESOLVED-CHARACTERIZED: real & OOS-persistent but short-vol penny-in-front-of-steamroller, not a tradeable edge |
| 16 | Kalshi market-data WebSocket feasibility | `kalshi-ws-feasibility` | VERIFIED 2026-06-10 with configured creds: connected+subscribed, ~410 book updates/s, median recv age 14ms, sub-second available; `KALSHI_HIRES_BOOK_SOURCE=auto` now active (WS preferred, REST fallback) | RESOLVED — WS in use |
| 17 | Deribit vol/options as model features | `record-deribit`, `DERIBIT_*` flags | Joined point-in-time as optional features; no standalone edge shown; ablation delta-Brier ≈ −0.0001 | INFRA — optional, off by default |
| 18 | Smaller-cap 15m series are less efficiently priced than BTC | `collect_all_series.ps1`; KXETH15M/KXSOL15M/KXDOGE15M/KXXRP15M | All four series live + parse identically (verified 2026-06-10); underlying spot/perp feeds wired per asset; collection starts 2026-06-10 — spread/calibration comparison vs BTC is the first analysis once windows accumulate | OPEN — collecting |
| 19 | Historical trade tape closes the maker-fill question NOW (no waiting) | `kalshi-backfill-trades` | DONE: ~600-700k KXBTC15M prints/day (~17% of the whole Kalshi tape), 0 errors/dupes, idempotent. NOTE: the endpoint is GLOBAL (client-side series filter) → future multi-series backfills should make ONE global pass writing all five series at once | INFRA — tape on disk Jun 1-10 |
| 20 | Sub-second WS data usable end-to-end (not just the v2 stale-quote study) | `--feature-source hires` on `kalshi-build-model-dataset` / `-data-readiness` / train / calibrate / backtest / gate; `feature_source.py` | DONE 2026-06-14: `joined_to_feature_row()` adapts `kalshi_hires_joined_snapshots` into the `kalshi_feature_rows` schema; one `--feature-source {rest,hires}` flag flows via `config.feature_source` to every consumer; REST default unchanged. Real-data proof: DOGE hires dataset TRAINING_READY (269,750 rows / 164 windows). Hires carries a feature SUBSET (no Kalshi bids/depth, CVD/OFI, Deribit, long-horizon vols — nulled + surfaced by missingness). Full suite 644 green | INFRA — second-level pipeline live |
| 21 | Hires-cadence outcome model beats market after cost (sub-second microstructure sharpens the 15m binary) | `scripts/research/hires_microstructure_lab.py --mode model`; walk-forward logistic on z/momentum/basis/size_imb vs market-implied | CONCLUDED-NEGATIVE 2026-06-14 across ALL 5 series (n=146–349 windows): market-implied is best-calibrated everywhere (ECE 0.024–0.057); every learned model worse OOS (ΔBrier +0.006…+0.047); after-cost divergence-taker negative on all 5 (SOL t−2.75). Φ(z) physics ties market on Brier but no tradeable divergence. Not regime luck — 5 independent assets agree | CONCLUDED-NEGATIVE |
| 22 | Sub-second signals predict the next underlying move (microstructure structure exists) | `hires_microstructure_lab.py --mode ic` | TRUE but NOT tradeable here: BTC IC mom_1s +0.195, size_imb −0.126, basis_chg_5s +0.099 (p≈0, n=1M), replicated DOGE. Predicts the 1–5s CEX spot/perp move, which you can't trade via a 15m Kalshi binary; `z_fair` (maps to outcome) IC≈0. Bridge = reprice-lag (#23), which is net-negative | INFRA — structure real, no Kalshi P&L bridge |
| 23 | Reprice-lag (stale-quote) survives with sub-second WS book | `kalshi-reprice-lag-report --hires` (re-run 2026-06-14 fresh data) | WS book UNBLOCKED the short horizons (coverage +250/500ms 0%→~87%). Still no_edge: DOGE win 19% pf 0.55; BTC win 24% avg −0.0016c/contract **pf 0.99 (break-even)**, 3bps cell marginally + but flips negative at 5/8/12bps. BTC is the closest-to-break-even result in the whole project | OPEN (watch) — re-run after ≥1wk of PURE WS-book joined data (current corpus mostly REST-cadence book); escalate only if pf>~1.1 t>2 robust |
| 24 | Deep-favorite 90–98c + high-\|z\| taker cell (as a TAKER) | `hires_microstructure_lab.py --mode favorite-pool` (pooled n=1002) | CONCLUDED-NEGATIVE 2026-06-14: bulk 0.98+ band (n=450) well-calibrated, taker loses the fee (t−3.0); 0.90–0.95 favorites mildly OVERpriced (−3.98c); only positive cell 0.80–0.90 hi\|z\| +1.73c but t0.4/n41 and 73% NO-favorites (not YES bias). Favorite yes%~47–51% = no directional tilt. the deep-favorite cell's positive was MAKER spread-capture, not a taker mispricing | CONCLUDED-NEGATIVE for taker; maker lens (#13) still open |
| 25 | Adverse-selection-aware maker: cancel a resting quote on sub-second momentum before an adverse through-fill (does EXP2's real signal rescue the maker lens?) | `scripts/research/adverse_maker_lab.py` (KXBTC15M, 6 days, 362–364 windows, real prints + joined snapshots, through-queue) | CONCLUDED-NEGATIVE 2026-06-14: crux FAILS at lead 120/240/360s (cancelled-fill EV NOT worse than survived; cancel-aware ≤ baseline ~−1.7c). `mom_1s` (real on the underlying, #22) does NOT identify which maker through-fills lose at the 15m binary. Robust+monotonic: through-fill adverse selection GROWS with earlier entry (naked single legs −8.4→−28.8c, all t<−6) and momentum-cancel makes them WORSE. Confirms #13/#14; extends EXP1/2/3 bridge-failure to the maker lens. Lone positive corner = lead≈450s (window-open) survived +1.08c but t+0.55 (insignificant), 1 of 4 leads, θ-fragile = multiple-comparison artifact | CONCLUDED-NEGATIVE; lead≈450s a LOW-confidence watch (forward data + non-momentum signal) |
| 27 | Alpha Discovery Engine (ADE) — automated, leakage-safe, multiple-testing-aware search for edge in the 15m binary after cost | `src/btc5m/discovery/` (metrics/gauntlet/cpcv/holdout/registry/selfcheck); `kalshi-ade-selfcheck`, `kalshi-ade-vault` | M1 RIGOR SPINE DONE 2026-06-14: Deflated Sharpe Ratio + PBO (CSCV) + parameter-plateau + cross-asset-replication gauntlet, combinatorial PURGED CV, hash-pinned SEALED holdout vault, append-only trial registry (cumulative DSR deflation). SELF-VALIDATED: rejects in-sample-best of pure noise (DSR 0.512, PBO 0.58) and accepts a genuine edge (DSR 1.000, PBO 0.00). 28 tests green. Vault on real BTC: 961 windows → 707 search / 240 holdout. M2 (feature factory + screen + after-cost rule search + CLI `kalshi-ade-mine`) DONE 2026-06-14. FIRST MINE (BTC): engine surfaced the dataset's most seductive pattern — distance/return-since-start → buy-favorite, +1.37c/trade **t=+5.55** — and the GAUNTLET REJECTED it (DSR 0.127≪0.95; threshold SPIKE). **M3 execution-realism DONE 2026-06-15** (fills priced at a quote `--exec-lag-seconds` AFTER the signal, default 3s; `panel.build_panel`). exec-lag SWEEP {0,3,10,30s}: candidate does NOT collapse (survives 30s lag, per-trade +1.6–2.1c, t 3.4–5.9) → it is **NOT** the reprice-lag/stale-quote artifact the first hypothesis was (corrected); leading hypothesis now the documented **YES-favorite/side bias #15** (which failed forward). Gauntlet REJECTS at EVERY lag (DSR 0.00–0.25, plateau spike) — false-positive control held even when the initial diagnosis was wrong. 33 discovery tests green | INFRA — engine live + exec-realistic; no edge blessed; OPEN: effective-N + candidate autopsy (#15 side-bias?) + wider factory |
| 28 | ADE other search domains: cross-asset (pooled 5-series) + execution/maker edges (the 'unchosen' paths) | `kalshi-ade-mine-pooled`, `kalshi-ade-mine-maker`; `scripts/research/autopsy_maker_candidate.py`; report `reports/ADE_OTHER_PATHS_20260615.md` | DONE 2026-06-15. **Cross-asset** (1524 pooled windows, per-asset rank-normed feats): best rule +EV on ALL 5 assets but each t<1.2, pooled window-Sharpe 0.051 → GAUNTLET REJECTS (DSR 0.001, spike) = 5/5-positive-but-underpowered near-miss. **Maker** (BTC prints-through): first candidate to clear DSR 0.998 + PBO 0.00 (rest YES bid on 96c+ favorites) — see #15: persists on sealed holdout but is short-vol favorite-capture (−96c tail), rejected by plateau gate. Cross-horizon mining BLOCKED (no KXBTCD history). Engine-roadmap: DSR blind to unrealized fat tail → add payoff-asymmetry/max-loss gate + real queue model | INFRA — both domains mined, NO tradeable edge; #15 characterized |
| 26 | Cross-horizon study (leg 26): KXBTC15M (15m up/down) vs KXBTCD (hourly BTC strike ladder) implied-vol coherence — same underlying/venue/CF-Benchmarks settlement | `scripts/research/xhorizon_discovery.py` + `xhorizon_probe_directional.py` (discovery); `xhorizon_vol_coherence.py` (probe/recorder) | DISCOVERY DONE 2026-06-14: KXBTCD is a LIQUID hourly ladder (188 strikes $100 apart, 1c spreads, 121 two-sided, ATM vol24 ~108k / OI ~43k) → reads a full implied terminal CDF. σ extraction works at healthy maturity (ladder slope → σ₁ₕ≈41% annualized; 15m σ via GTE inversion). Coherence verdict NEEDS DATA: no KXBTCD history is recorded; concurrent short-ladder + ATM-15m samples must accumulate (ladders also have maturity gaps). `xhorizon_vol_coherence.py` records JSONL to data/xhorizon/coherence/ — loop it alongside collectors | OPEN — infra built, accumulating |

| 29 | Cross-coin relative value (BTC leads alts) — first CROSS-SECTIONAL bet | `kalshi-ade-mine-crosscoin` (crosscoin.py); report `reports/ADE_PATHS_1_2_3_20260615.md` | DONE 2026-06-15. REAL lead-lag: BTC concurrent implied-prob predicts alt OUTCOME IC +0.63 AND alt market-RESIDUAL IC **+0.28** (stab 0.80) — strongest residual IC any mine produced; BTC genuinely leads alts & alt markets are slow to price it. BUT gauntlet REJECTS (DSR 0.024, **PBO 0.59 overfit**, spike; per-asset mixed) — alt spreads 3-4× BTC's eat the thin residual. Real signal, no cost-clearing bridge (now cross-sectional) | CONCLUDED-NEGATIVE (untradeable); cross-coin IC is the watch item IF alt liquidity improves |
| 30 | Strike-ladder internal arbitrage (live KXBTCD/alt ladders) | `scripts/research/ladder_arb_scan.py` | DONE 2026-06-15: NO ARB. Directional ≥K ladders (KXBTCD 188-strike, KXSOLD) tight at 1.0100 (=1c spread), monotonic, consistent; range series bucket-asks sum 2-3 (no buy-all). Strike-type-aware (a naive first pass false-flagged range markets; caught via the clean ladder reading 1.01). Reusable scanner; KXBTCD confirmed clean+liquid for the cross-horizon study | CONCLUDED-NEGATIVE (snapshots); could re-run continuously for transient dislocations |
| 31 | Settlement-TWAP final-60s mechanic (60s-avg settle partially frozen near close) | `scripts/research/twap_settlement_probe.py` | DONE 2026-06-15: NO EDGE. Frozen partial-average distance ≡ instantaneous spot distance (IC vs outcome 0.865 vs 0.864; vs residual 0.629 vs 0.629) — averaging adds zero info (spot ~constant over final 35s vs the distance). Residual IC is the known late-window distance/reprice-lag (#8/#23/deep-favorite), not TWAP-specific; after-cost n=3 −33c | CONCLUDED-NEGATIVE |

| 32 | Regime-CONDITIONAL edge (does efficiency break in a specific regime?) + volume/Deribit features | `kalshi-ade-mine-conditional` (regimes.py + conditional.py); report `reports/ADE_CONDITIONAL_AND_CROSSCOIN_GAP_20260615.md` | DONE 2026-06-15: NO conditional edge. 21 regime cells (vol/spread/depth/volume/Kalshi-size/trend/time-of-day/IV), 3988 trials, DSR deflated by ALL cells → every cell DSR 0.00. Seductive pockets (tod=h00 +17.8c, spread=hi +15.1c) = multiple-comparison mirages, all rejected by global deflation. Market efficient ACROSS regimes, not just on average. VOLUME fully mined (ask_size/top_size/depth/intensity 92-100% cov) = no edge; DERIBIT only 18% covered (real but thin window) = nothing, needs forward collection to use | CONCLUDED-NEGATIVE (efficient conditionally too) |
| 33 | Cross-coin tradability GAP — how far from tradeable is the BTC→alt lead, per alt? | `scripts/research/crosscoin_gap.py` | DONE 2026-06-15: gap scales with spread. **KXETH15M already nominally +1.23c/trade after cost** (residual IC +0.335, 1c spread — wins on both axes) but **t=+0.56 (noise), and failed pooled gauntlet (PBO 0.59)**. SOL −1.08c (needs 2.2c tighter), DOGE −2.70c (5.4c), XRP −3.77c (7.5c). ETH = the one cell a coin-flip from real | OPEN-WATCH — accumulate ETH forward windows; re-test through gauntlet; if +1.2c holds t>2 = first real edge candidate |

| 34 | Multi-feature COMBINATION beats the price after cost (did single-feature mines leave signal on the table?) | `kalshi-ade-mine-combo` (combo.py) — walk-forward logistic on top-K residual-IC features, after-cost divergence trade, gauntlet | DONE 2026-06-15: NO EDGE, and the most telling result. Best combo (logistic top-5, div≥0.08): per_trade **+0.60c t=+0.19 ≈ ZERO**, window-Sharpe 0.007; DSR 0.182, PBO 0.53, spike. Unlike single-feature rules (fake +1.5-4c stale-quote artifact), the COMBINATION ~exactly reproduces the market price (|p_model−mkt|<thresh mostly) → nothing left to trade. Direct proof the price sits at the information frontier of the ENTIRE feature library | CONCLUDED-NEGATIVE (capstone: combination = market) |

| 35 | Print-level microstructure (large-print/sweep/toxicity, finer than aggregate CVD) | `scripts/research/print_microstructure_mine.py`; report `reports/ADE_FINAL_STONES_20260615.md` | DONE 2026-06-15: NO EDGE. `p_large_signed` (whale signed-vol) residual IC +0.063 (real but an order of magnitude too small); best rule = distance artifact, DSR 0.525 spike. Order flow already priced | CONCLUDED-NEGATIVE |
| 36 | Cross-sectional reversion (fade alt deviation from BTC factor) | `scripts/research/crosssectional_reversion.py` | DONE 2026-06-15: NO EDGE. dev features (alt dist-z − BTC dist-z) don't surface in top residual IC; best rule overfit PBO 0.73, DSR 0.184, spike. The opposite-structured cousin of lead-lag #29 also dead | CONCLUDED-NEGATIVE |
| 37 | Value of SPEED — does faster execution unlock the reprice-lag? | `kalshi-reprice-lag-report --hires` (1.37M rows fresh) | DONE 2026-06-15: NO (for taker). Sub-second covered (+250/500ms 89%), still net −0.027c/contract pf 0.82, negative all thresholds. KEY: Kalshi follows BTC's direction only 58% of shocks with **median lag 0.13c** — quote barely stale, edge ABSENT not latency-gated. Faster than 250ms changes nothing. NUANCE: speed could still matter for a MAKER (fast cancel/requote vs #14 adverse selection) — but that needs LIVE infra, untestable offline | CONCLUDED-NEGATIVE for taker; maker-speed OPEN but needs live |

| 38 | Round-trip MARKET-MAKING (spread capture, not buy-and-hold) + value of cancel/requote SPEED | `scripts/research/market_making_sim.py` (cancel-latency sweep); report `reports/ADE_MARKET_MAKING_20260615.md` | DONE 2026-06-15: NOT VIABLE, and speed makes it WORSE. With queue cap + inventory skew (static reproduces #14's −1.76c): EVERY maker fill is ~0.6-0.9c adversely selected (spread captured < toxicity); more/faster quoting = more toxic fills = bigger loss (250ms −717c vs 4s −201c vs static −1.76c). DEEPEST efficient result: MM needs a FAIR-VALUE edge to avoid toxic fills; project proved there is none (price=best forecaster, combo #34 re-derives it) → every fill adverse → speed just gets you adversely selected faster. Kalshi has no maker rebate. Only maker positive ever = deep-favorite short-vol #15 (not scalable) | CONCLUDED-NEGATIVE; closes the fast-maker thread on the merits |

## What is NOT worth doing (without new inputs)
- Re-training bigger/fancier models on the same ~1–4s REST data. Three model
  families and a residual reformulation all converge to "the price is better."
- Re-running shadow experiments hoping passes accumulate (leg 6 explains why).
- Threshold/buffer tuning to "let trades through" — the buffers are measuring
  real bias, not being conservative for fun.

## What unlocks new information (in value order)
1. ~~Kalshi API key~~ **DONE 2026-06-10** — creds were already in `.env`; WS
   verified (~410 upd/s, 14ms) and the hi-res recorder now uses it
   (`KALSHI_HIRES_BOOK_SOURCE=auto`). Legs 10/13/14 need accumulated WS data,
   then re-run their studies.
2. ~~Public trade prints~~ **DONE 2026-06-10** — the collector records
   `kalshi_trades` rows (taker_side included) every ~5s.
3. **Forward verification of leg 15** (deep-favorite YES) — STRENGTHENED by real
   fills (YES 90c+: +2.53c/fill certain-bound, 191 windows; 80-90c also +1.6c) and
   it is the only cell positive across BOTH up and down days; still verify on
   post-June-10 data before sizing.
4. **Cross-venue (Polymarket BTC) price comparison** — needs the parked account
   funded; the 5m pipeline is in git history if revived.
